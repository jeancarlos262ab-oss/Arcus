# Display-only values for the standalone mock dashboard.
# The deployed backend remains the source of truth for runtime configuration.
VITE_ARCUS_AWS_REGION=us-east-1
VITE_ARCUS_BEDROCK_MODEL_ID=us.amazon.nova-2-lite-v1:0
